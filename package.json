{
  "name": "interview-prep",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,electron -c blue,green \"npm run dev --prefix client\" \"wait-on http://localhost:5173 && npm run dev --prefix desktop\"",
    "build": "npm run build --prefix client && npm run build --prefix desktop",
    "start": "npm run start --prefix desktop"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.3"
  }
}
